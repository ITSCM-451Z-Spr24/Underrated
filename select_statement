SELECT 
    p.playerID,
    p.nameFirst,
    p.nameLast,
    ((b.H - b.HR) / (b.AB - b.K - b.HR + b.SF)) + b.OPS + b.SB AS offensiveScore,
    (pt.W + pt.GS) AS pitchingScore
FROM 
    People AS p
JOIN 
    Batting AS b ON p.playerID = b.playerID
JOIN 
    Pitching AS pt ON p.playerID = pt.playerID;
